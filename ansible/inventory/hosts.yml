all:
  children:
    homeservers:
      hosts:
        mediaserver:
          ansible_host: 192.168.1.50 # Replace with your server's IP address
          ansible_user: mediauser    # Your sudo-enabled user