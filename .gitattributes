# Shell scripts and templates must keep LF line endings, even when the repo
# is cloned on Windows — CRLF breaks them on the server / in WSL.
*.sh text eol=lf
*.j2 text eol=lf
*.yml text eol=lf
*.cfg text eol=lf
